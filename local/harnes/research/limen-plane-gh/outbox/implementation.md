# REZ seat write-back candidate

Candidate published for review as [kulfix/limen PR #11](https://github.com/kulfix/limen/pull/11), with its link and result verified in REZ-138. Implements approved variant B from [the decision](../to-limen.md): project-scoped external tracker truth, one coordinator writer and a dependency-free Plane REST seat client. The convention starts at [PLANE-GH.md](../../../PLANE-GH.md), linked from the harnes index. No Limen runtime, global role prompts, Adam board/features, Linear, F014 or pytek product code changed. No sync engine, worker spawning, merge, deployment or automatic finish ping.

Actual implementation model: **`openai-codex / gpt-6-astra`, reasoning `high`**, reported by this job's `PI_PROVIDER`, `PI_MODEL` and `PI_REASONING_LEVEL`. No fallback or secondary model was used.

## Delivered behavior

`local/harnes/plane.ts` supports WI/states GET, comment/link POST with stable `limen:<job-id>:<operation>` markers, and state PATCH with a known destination and expected source state. It follows cursor pagination, searches all pages before each POST, reconciles ambiguous writes by GET, bounds rate-limit attempts, refuses auth errors and prints verified receipts only after read-back. State preconditions are advisory REST checks, not atomic compare-and-swap. Concurrent writers, server-delayed visibility and server HTML rewriting remain explicit limits; conflicts stay pending, never falsely done.

`test/plane.test.ts` is in the native test/typecheck lane. The CLI accepts an explicit outside-repo config/key file or opt-in environment variables from the Ops-named environment file. It never discovers secrets, follows redirects or logs HTTP response bodies on failure. The project convention covers GitHub's existing `gh` seat, partial delivery across trackers, source snapshots and terminal acceptance; no GitHub client or issue synchronization is added.

## Authorization correction and live evidence

The original handoff's `kulinski/limen` 404 and authenticated `kulfix` mismatch were retained until the user explicitly corrected the PR target to **`kulfix/limen` (origin)**. Authenticated repository GET then succeeded. The same user message declared Plane auth ready, named `~/.config/plane/env`, and authorized REZ-138 write smoke. Only that named file was sourced, without tracing or printing credentials. This job acted as the sole delegated smoke writer.

Ops supplied `https://plane.wczasowa8.pl/api/v1`, workspace `wczasowa8` and project `47b7d43a-9fc1-4dce-9c51-863e09c108c1`. Live GET confirmed REZ-138, WI `2f532bb9-d9c5-436f-b03a-5fde182a22fa`, “Limen ↔ Plane/GH write-back (wariant B)”; GET states returned five workflow states. The WI was in Backlog (`976c22aa-3e76-4dda-ba6d-9c67f4709682`). No browser WI URL was supplied or returned; none was invented.

A marked smoke comment was POSTed and verified by GET: comment ID `1c0dd481-febc-4684-8d38-1e29a147f800`, marker `limen:2026-09-15-plane-tracker-write-back-5849fe43:smoke`. Running the identical request again returned the same comment ID. The comment explicitly says implementation/PR and acceptance are pending, not Done. The authorized PR was created on `kulfix/limen` and read back as OPEN: https://github.com/kulfix/limen/pull/11. Its marked Plane link was verified by GET and rerun returned the same link ID `0e8037bc-a591-4023-bde7-168091808074` (marker `limen:2026-09-15-plane-tracker-write-back-5849fe43:pr`). A separate result comment, ID `56e7d605-a770-4936-9bb2-616f8b658161`, publishes the PR, passing stub tests and failing native checks without claiming Done. Final GET confirmed the WI state is unchanged. No live state PATCH was made because no destination transition was agreed. REZ-138's source text currently names `kulfix/pytek`, unlike the original handoff; obtain an explicit authorized issue URL before any GitHub code-issue write.

## Checks and retained artifacts

- `npm ci`: installed six packages from the lockfile; audit reported zero vulnerabilities.
- Discriminating first check: `node --test --test-name-pattern='accepted POST timeout' test/plane.test.ts` — **2 passed**. The real loopback stub stored a comment/link and withheld the POST response; read-back and rerun found its marker on page two, with exactly one POST for each operation.
- `node --test --test-timeout=10000 test/plane.test.ts` — initial **27 passed**, then **30 passed** after API-root, redirect, array-list and environment CLI coverage. Negative stubs returned 200 without storing writes, refused read-back and caused partial CLI delivery; these yielded pending errors/nonzero exit, empty success output and no state advancement. Also covered 401/403, bounded 429, malformed/repeated pagination, marker conflicts and PATCH timeout dedupe.
- `npx biome check --write local/harnes/plane.ts test/plane.test.ts`: only the two new TypeScript files formatted; final focused run checked two files with no fixes.
- `git diff --check`: passed before candidate commit.
- Full native `npm run check`, run once on clean code candidate `38c9475`: **exit 1**. TypeScript passed. Biome checked 98 files and reported six formatting errors in untouched files: `local/harnes/archive/runtime/.pi/settings.json`, four `local/harnes/archive/sources/` JSON files (`3pSATWHe2W4.metadata.json`, `discord-pstack-source-manifest.json`, `poteto-pstack-pt1.json`, `poteto-pstack-pt2.json`), and `test/spawn-command.test.ts`. The combined command therefore did not reach tests. No unrelated formatting was applied.
- `npm test`, run separately on the same clean candidate: **443 tests, 440 passed, 3 failed, no cancellations** (422 seconds). `communication-hook.test.ts` and `init-command.test.ts` each require two revisions of `templates/reviewer.md`; this clone's Git log contains only `38b2dc9` for that path. `inherit.test.ts` reports a shipped `judge.md` history date of 2026-09-04 versus this clone's expected 2026-09-14. These failures are outside the diff; histories were not regenerated. Full logs retain the assertions.
- After the native run, only documentation and retained evidence were amended into the single implementation commit; tested TypeScript code is unchanged. Final focused Biome checked four changed TypeScript/JSON files with no fixes. The first evidence whitespace check failed on trailing spaces emitted by the native reporters; committed native logs trim only that trailing whitespace, while byte-original logs and the failed whitespace check are retained externally under `raw/`. The corrected staged whitespace check passes. A secret-safety check confirmed the provisioned API key is absent from the staged candidate diff.

Check logs and sanitized smoke receipts are retained beside this report in `evidence/`. A reviewer-readable copy outside this worktree is `/srv/limen/tools/limen/.limen/jobs/2026-09-15-plane-tracker-write-back-5849fe43/plane-evidence/`. Raw local smoke responses and requests are under `/tmp/plane-tracker-write-back-5849fe43-evidence/`; no secret is part of retained receipts.

## Continuation

Branch: `limen/2026-09-15-plane-tracker-write-back-5849fe43`; worktree: `/srv/limen/tools/.limen-limen-worktrees/2026-09-15-plane-tracker-write-back-5849fe43`. Session pointer: `/srv/limen/tools/limen/.limen/jobs/2026-09-15-plane-tracker-write-back-5849fe43/session`.

PR #11 is open on the explicitly authorized repository; no merge was performed. The PR includes the coordinator's existing decision-doc parent `8af7a2d` plus this one implementation slice; this worker did not modify `notes.md` or `to-limen.md`. Remaining operator work: canonical browser WI URL, agreed live state transition, any authorized GitHub code issue and owner review/acceptance. The native lane is not green: resolve the repository formatting/history baseline separately if required for merge, without treating the 30 passing focused stubs as a full-suite pass. No completion-state change is authorized by a passing smoke. Automatic finish webhook is disabled; no manual ping was sent.
