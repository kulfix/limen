# Limen board (day one)

Static HTML board + `status.json`. Install to `/srv/limen/board` on the seat.

- Viewer: `index.html` (NOW / NEXT / PROVEN)
- Example schema: `status.example.json`
- Live `status.json` is produced only by `../scripts/board-aggregate.sh`

See `../procedures/limen-board.md`.
