# NO-GO → GO on the VPS

The accepted cut stays unreleased until all three gaps below close and Adam reviews it. This coordinator alone lands; workers use `openai-codex / gpt-6-astra / high`, without finish sends or model fallback.

1. **Me — native verification closed (F709).** Test-only registry and fake-Herdr corrections landed `2181d35`; the repaired candidate passed TypeScript, Biome and all 370 tests. Real process overlap, dead-lock reclamation, zero settled-record reads, running-job visibility and existing timeouts remain required. Both failed and successful full lanes are retained in the completed feature's `checks-1.md` / `checks-2.md`.
2. **Me + Johnny — prove one automatic finish (F091).** Prepare an executable export-hold harness outside the trusted receiver source. Johnny privately selects his single route and supplies real history/export access before a send. One hosted proof job sends automatically while HTTP stays enabled; capture accepted/unobserved views with the actual export held, then atomically release that same authorized export and capture observed views with unchanged transport receipts. Johnny attests history, exact event, completion time and one ingress delivery. No paused webhook, synthetic turn, Tony or manual duplicate. Missing prerequisites become exact commands in `ASK-JOHNNY.md`, not repeated sends.
3. **Pi; Adam only for a changed spend policy — unblock route refusal (F081).** Wait for a supported non-generating check of the exact provider/model/API route and its refusal reason. Me: retain the current interface evidence and implement/test refusal only when that capability exists. Alternative, not authorized: Adam explicitly bounds generated-probe spend/tokens, wall time and Pi-owned retries. Auth readiness/catalog membership and guessed zero-cost generation cannot close this gap. Waiting is recommended and keeps NO-GO.
4. **Me → Adam — final cut review.** Reconcile the board, verify committed code and actual Johnny proof, and replace the three gap lines with cited results. Adam reviews the cut and real receiver evidence. Only then may readiness say GO; this task does not authorize tagging, publishing or pushing.

Native verification and local proof preparation are delivered; Johnny can run the receiver proof while Pi is blocked. No implementation jobs remain running. Workers remain sequential; Johnny's file export is receiver-owned, not another implementation lane.

## Stay deferred

Wake ceiling (F090) remains locked at `0fa48fa`: no merge, rebase, repair or widening without Adam. Author routing (F708), field guide, Claude advisor, closing overview, history retirement, remote-seat rollout and GitHub doorbell remain beyond narrow 1.0. No new control plane, receiver API, identity service or lowered release scope.
